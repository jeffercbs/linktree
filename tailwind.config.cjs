const config = {
   content: ["./src/**/*.{html,js,svelte,ts}"],

   theme: {
      extend: {
         colors: {
            primary: "#1c1917",
            brawnis: "#292524",
            grey: "#e9ecef",
         },
      },
   },

   plugins: [],
};

module.exports = config;
