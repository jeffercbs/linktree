const config = {
   content: ["./src/**/*.{html,js,svelte,ts}"],

   theme: {
      extend: {
         colors: {
            primary: "#1750FEff",
            dark: " #ffffff00",
            emeral: "#06B863ff",
            grey: "#e9ecef",
         },
      },
   },

   plugins: [],
};

module.exports = config;
